// tagent CLI 入口
// Step 5 才会实现完整 REPL，现在只验证 workspace 依赖贯通
import { CORE_VERSION } from "@tagent/core";

console.log(`tagent cli 启动成功，core 版本 ${CORE_VERSION}`);
console.log("（Step 1 骨架阶段：CLI 尚未实现，等待 core 各模块就绪）");
