{
  "name": "tagent-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "从零手搓的本地 Agent（学习项目）",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "cli": "pnpm --filter @tagent/cli dev"
  }
}
